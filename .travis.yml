language: cpp

compiler:
    - gcc
    - clang

install:
    - sudo apt-get install protobuf-compiler libprotobuf-dev
    - sudo apt-get install qt5-qmake qt5-default
    - qmake -v

script:
    - ./setup.sh
    - cd src
    - qmake panoramix.pro
    - make CC=$CC CXX=$CXX

